import 'package:flutter/material.dart';

/// Centralized color palette for DUO AI.
/// Kept separate from the theme so agent-specific accent colors
/// (Developer / Marketing / Manager) can be added here later.
class AppColors {
  AppColors._();

  static const Color primarySeed = Color(0xFF6750A4);
}
